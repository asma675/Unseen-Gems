# WorldGems Map

A glow UI map explorer with:
- Real map (Leaflet + OpenStreetMap)
- Search any city/state/country
- Categories + Hidden Gems mode
- Dark/Light mode

## Run locally
```bash
npm install
npm run dev
```

## Deploy (Vercel)
- Build command: `npm run build`
- Output directory: `dist`
